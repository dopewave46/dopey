# DopeOrca OS — frontend environment
# Copy to `.env` for local overrides. `.env` is git-ignored.

# Base URL of the backend API (server/). Defaults to this value when unset,
# so a standard local setup needs no .env at all.
VITE_API_URL=http://localhost:4000/api
